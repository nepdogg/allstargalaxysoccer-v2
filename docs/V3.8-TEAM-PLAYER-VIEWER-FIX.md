# Allstar Galaxy V3.8 — Team Player Viewer Fix

Team-page-only update. The shared framework remains locked.

## Fixes
- Moves the player modal directly under `<body>` so it is a true viewport-fixed overlay.
- Fixes the popup appearing far below the visible screen at normal browser zoom.
- Keeps both Front Card and Profile Card visible together in the player viewer.
- Clicking either card opens a dedicated full-size card lightbox.
- ESC closes the full-size card first, then the player viewer.
- Preserves previous/next player controls.
- Fixes the roster track transform so horizontal movement no longer removes vertical centering.
- Keeps the active carousel card fully centered instead of dropping/cropping toward the bottom.

## Files changed
- `js/pages/team.js`
- `css/pages/team.css`
